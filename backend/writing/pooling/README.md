# pooling

The one place cross-district code lives. Two tables, no job yet.

Read `CONTRACT.md` before adding anything. The short version: loop over districts rather than
joining across them, consent gates entry to the frame, and nothing that crosses carries an
identifier.

Design: the SIP teacher-subsystem expansion plan section 7, and the LDC PM console user design
section 2.
