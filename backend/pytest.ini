[pytest]
# Run from backend/ (or repo root): `python -m pytest`. conftest.py puts backend/ on
# sys.path so tests can `import etl...` / `import app...` the way the loaders expect.
testpaths = etl app
addopts = -q
