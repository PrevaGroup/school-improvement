# registry

The nodes, the tasks, which iterations are scored on which nodes, and the scoring configuration.
Plus the linter - the fourth enforcement mechanism, after the boundary test, the route contract test
and the eval loop.

Read `CONTRACT.md` before changing a table shape, especially the node rule. The short version: the
identifier is the identity, the scale belongs to the node, and nothing is harmonised.
