# Test/dev-only deps. Runtime deps are in requirements.txt (the tests import the
# real ETL modules, so those must be installed too):
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest
-r requirements.txt
pytest
# fastapi.testclient (tests/test_spa_fallback.py) needs httpx. It arrives transitively via
# anthropic today, but a test dependency that only works by accident is one `pip` resolution
# away from a confusing red. Declare it.
httpx
