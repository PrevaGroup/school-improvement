fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # multipart/form-data for POST /plans/extract uploads
SQLAlchemy>=2.0.30
alembic>=1.13
psycopg[binary]>=3.1
pydantic-settings>=2.2
# Google OIDC verification for the tenant-binding (see app/security.py)
google-auth>=2.29
# DB credentials from Secret Manager (runbook Phase 3)
google-cloud-secret-manager>=2.20
# ETL loaders read the raw data from local paths OR gs:// URIs (etl/ca/_shared.py)
fsspec>=2024.5
gcsfs>=2024.5
# SIP plan extractor: Claude reads the PDF natively; pypdf counts pages (etl/ca/sip/)
anthropic>=0.40
pypdf>=4.0
# "Schools Like You" peer matching: Ledoit-Wolf covariance + Mahalanobis kNN (etl/peers/)
scikit-learn>=1.3
# Cloud Run -> Cloud SQL without the Auth Proxy (app/db.py, when INSTANCE_CONNECTION_NAME set)
cloud-sql-python-connector[pg8000]>=1.9
pg8000>=1.31
