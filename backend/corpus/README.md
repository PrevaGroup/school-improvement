# corpus

The anchor papers. Load with:

    python -m corpus.load_persuade --data-dir <dir> --dry-run

Read `CONTRACT.md` first. The short version: no rater identity anywhere, no trait scores, the
holdout is within the corpus by paper, and the demographics are the reason two of the five stop
conditions can be tested before a real student's paper exists.
